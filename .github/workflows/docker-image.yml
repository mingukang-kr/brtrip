name: Test with Docker CI

on:
  push:
    branches: [ feat/test-docker-action ]
  pull_request:
    branches: [ feat/test-docker-action ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
